# Other-cluster placeholder

This folder intentionally has no configured account, modules, or storage paths.
Use Codex on the destination cluster to copy/configure this folder after
inspecting that site's environment. Follow [the cluster setup guide](../README.md#placeholder-configure-the-other-cluster-with-codex).

Do not reuse Fir's Python environments or assume its CUDA modules, account, GPU
names, or shared paths exist here. Configure the five repo-local input/output
links, rebuild the environments, and verify dry runs before submitting tests.
